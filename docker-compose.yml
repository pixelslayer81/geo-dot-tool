version: "3.9"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Persist Natural Earth shapefiles so they aren't re-downloaded on restart
      - geodata:/app/data
    environment:
      PORT: 8000
    restart: unless-stopped

volumes:
  geodata:
